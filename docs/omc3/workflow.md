# OMC3 Sotfware Workflow

!!! note
    Section to explain the reason behind omc3 workflow (integration with GUIs etc).
    Go through exactly what happens when we run `hole_in_one`, which steps, files, etc.